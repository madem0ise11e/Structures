#include <stdio.h>
#define MAXLEN 50

struct studente {
char cognome;
int votoBasso;
int votoAlto;
float mediaVoti;
};

int main() {
  struct studente s[MAXLEN];
  printf("Inserisci qualche n: ");
  scanf("%d", &n);
}
